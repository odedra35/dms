# ansible-galaxy collection install community.docker
---
  - name: Install Docker
    apt:
      name: docker.io
      state: present
      update_cache: yes

  - name: Run DMS app
    docker_container:
      name: DMS
      image: davdez1/dms:Jan2025
      state: started
      restart_policy: always
      detach: true
      recreate: yes
      ports:
        - "8080:8080"